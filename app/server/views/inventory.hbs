<h1 class="sr-only">
    Beschikbaar materiaal
</h1>

<section class="container">
    <h2>
        Beschikbaar
    </h2>

    <div>
        <ul>
            {{#each inventory}}
                {{!-- {{#if}} --}}
                    <li>
                        <a href="#{{this.collection.label}}">
                            {{this.collection.label}}
                        </a>
                    </li>
                {{!-- {{/if}} --}}
            {{/each}}
        </ul>

        <a href="/plan-opstellen" class="btn btn--tertiairy">
            Plan opstellen
        </a>
    </div>

    <section>
        {{#each collections}}
            <h3>
                {{this.label}}
            </h3>

            <ul>
                {{#each inventory}}
                    {{#if (eq this.inventory.collection.id this.collection.id)}}
                        <li>
                            {{this.label}}
                        </li>
                    {{/if}}
                {{/each}}
            </ul>
        {{/each}}
    </section>

    <a href="link to ID">
        Beschikbare elementen uit database
    </a>
</section>

<section class="container--wide">
    De elementen opgelijst
</section>

<section class="block block--lightblue">
    <div class="container">
        <div class="block__text">
            <h2>
                Vraag aan
            </h2>

            <p>
                Ziet u een bepaald instrument niet staan of zijn er niet genoeg van een specifiek element? <span class="active span--inline">Aarzel dan niet om ons te contacteren!</span> Wij kunnen u misschien helpen met uw probleem.
            </p>

            <a href="/vraag-aan" class="btn btn--tertiairy">
                Ga naar aanvragen
            </a>
        </div>

        <svg class="circle-shape" xmlns="http://www.w3.org/2000/svg" width="599" height="599" fill="none"><circle cx="299.5" cy="299.5" r="298.5" stroke="var(--secondary)" stroke-width="2"/></svg>
    </div>
</section>